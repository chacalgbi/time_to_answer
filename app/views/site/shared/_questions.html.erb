  <div class="jumbotron">
    <% unless params[:term] %>
      <h3>Últimas perguntas cadastradas</h3>
    <% else %>
      <h3><%= "Resuldado para o termo \"#{params[:term]}\"..." %></h3>
    <% end %>
    <hr class="my-4">
  </div>

<% @questions.each do |question| %>
	<%= form_with url: site_answer_path, local: true do |form| %>
		<div class="card">
		  <div class="card-header">
		    <%= question.description %>
		  </div>
		  <div class="card-body">
		    <blockquote class="blockquote mb-0">
		      	<ul>
		      		<% question.answers.each do |answer| %>
		      			<li style="list-style: none;">
							<div class="radio">
							  <%= radio_button_tag 'answer', answer.id %>
							  <label><%= answer.description %></label>
							</div>
						</li>
		      		<% end %>
		      		<li style="list-style: none;"><%= form.submit "Responder", class:"btn btn-primary" %></li>
		      	</ul>
		    </blockquote>
		  </div>
		</div>
	<% end %>
<% end %>

<div class="text-center">
	<%= paginate @questions %>
</div>